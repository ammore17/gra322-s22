var slider = tns({
    container: '.slideshow',
    nav: false,
    items: 3,
    slideBy: 'page',
    mouseDrag: true,
  });

  var slider = tns({
    container: '.slideshow',
    nav: false,
    items: 3,
    slideBy: 'page',
    mouseDrag: true,
  });

  var slider = tns({
    container: '.slideshow',
    nav: false,
    items: 3,
    slideBy: 'page',
    mouseDrag: true,
  });