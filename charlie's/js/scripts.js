var slider = tns({
    container: '.hotdrinks',
    nav: false,
    items: 3,
    slideBy: 'page',
    mouseDrag: true,
  });

  var slider = tns({
    container: '.sandwiches',
    nav: false,
    items: 3,
    slideBy: 'page',
    mouseDrag: true,
  });

  var slider = tns({
    container: '.iceddrinks',
    nav: false,
    items: 3,
    slideBy: 'page',
    mouseDrag: true,
  });